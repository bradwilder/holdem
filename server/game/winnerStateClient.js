let WinnerStateClient = (winningCards, handDescription, winners) =>
{
	return {
		winningCards: winningCards,
		handDescription, handDescription,
		winners: winners
	}
}

module.exports = WinnerStateClient;
